import styled from '@emotion/styled';

export const Error = styled.p`
  color: red !important;
  font-weight: bold;
  font-style: italic;
  padding: 0.25rem;
  margin: 0;
  margin-bottom: 0.25rem;
`;
